server buf {
    var data : (0..2)[5];
    var count : 0..5;

    { put(elem : 0..2) | count < 5 } -> {
        data[count] = elem,
        count = count + 1
    }
}

process foo() {
    loop buf.put(1);
}
